const score = 999
console.log(score);

const balance = new Number(99)
console.log(balance);

console.log(balance.toString().concat);
console.log(balance.toFixed(4));

const otherNumber = 193.9386

console.log(otherNumber.toPrecision(3)); //precision method is used for finding out the precise value of a numbers


const hundreds = 100000
// console.log(hundreds.toLocaleString('en-IN ')); //changes the 100000 into 1,00,000 to from english to indian currency


//+++++++++++++++++Maths+++++++++++++++++++++++++++
console.log(Math);
console.log(Math.abs(1));
console.log(Math.round(99.99));
console.log(Math.ceil(999.99));

console.log(Math.random());
console.log(Math.min(4, 3, 2, 1, -1, 0));
console.log(Math.max(10, 20, 21, 45));

const min = 0
const max = 44

Math.random()

console.log(Math.floor(Math.random() * ( min-max + 1)) + min);
 
